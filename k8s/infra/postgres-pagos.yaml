apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-pagos
  namespace: filmstars
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-pagos
  template:
    metadata:
      labels:
        app: postgres-pagos
    spec:
      containers:
        - name: postgres-pagos
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: filmstars-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: pagos-service-config
                  key: DB_NAME
          volumeMounts:
            - name: postgres-pagos-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: [pg_isready, -U, postgres, -d, pagos_service]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: [pg_isready, -U, postgres, -d, pagos_service]
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: postgres-pagos-data
          persistentVolumeClaim:
            claimName: pvc-postgres-pagos

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-pagos
  namespace: filmstars
spec:
  selector:
    app: postgres-pagos
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
