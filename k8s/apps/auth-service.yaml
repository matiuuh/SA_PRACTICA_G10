# Imagen sustituida por envsubst en el pipeline: ${REGISTRY_HOST}/filmstars-auth-service:${IMAGE_TAG}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: filmstars
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      imagePullSecrets:
        - name: registry-credentials
      containers:
        - name: auth-service
          image: ${REGISTRY_HOST}/filmstars-auth-service:${IMAGE_TAG}
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: auth-service-config
                  key: PORT
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: auth-service-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: DB_PORT
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: DB_USERNAME
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: auth-service-config
                  key: DB_NAME
            - name: JWT_EXPIRES_IN
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: JWT_EXPIRES_IN
            - name: DEFAULT_ROLE
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: DEFAULT_ROLE
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: filmstars-secrets
                  key: POSTGRES_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: filmstars-secrets
                  key: JWT_SECRET
          readinessProbe:
            tcpSocket:
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: filmstars
spec:
  selector:
    app: auth-service
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
