apiVersion: apps/v1
kind: Deployment
metadata:
  name: funciones-service
  namespace: filmstars
spec:
  replicas: 1
  selector:
    matchLabels:
      app: funciones-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: funciones-service
    spec:
      imagePullSecrets:
        - name: registry-credentials
      containers:
        - name: funciones-service
          image: ${REGISTRY_HOST}/filmstars-funciones-service:${IMAGE_TAG}
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: funciones-service-config
                  key: PORT
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: funciones-service-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: DB_PORT
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: filmstars-config
                  key: DB_USERNAME
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: funciones-service-config
                  key: DB_NAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: filmstars-secrets
                  key: POSTGRES_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: filmstars-secrets
                  key: JWT_SECRET
          readinessProbe:
            tcpSocket:
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

---
apiVersion: v1
kind: Service
metadata:
  name: funciones-service
  namespace: filmstars
spec:
  selector:
    app: funciones-service
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: 3003
